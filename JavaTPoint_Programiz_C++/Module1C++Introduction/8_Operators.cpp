// Operators

// 1.Binary operators

// 1.1 Arithmetic   +, -, *, /, %

// 1.2 Relational   >, >=, <, <=, ==, !=

// 1.3 Logical      &&, ||, !

// 1.4 Bitwise       & - bitwise AND
//                   | - bitwise OR
//                   >> - right shift
//                   << - left shift
//                   ~  - bitwise not
//                   ^  - bitwise XOR

// 1.5 Assignament  =, +=, -=, *=, /=, %=

// 2. Unary operator        -- ++
// we can use --/++ operators as prefixes (--a, ++a) or postfix(a++,b++)
// prefix --a / ++a first we evaluate the increment/decrement value and after that we use it
// postfix a++/a--  first we use a and after that we evaluate the incremented/decremented value

// 3. Ternary Operator      ?:

// 4. Other Operators
// sizeof   - returns the size of dataType
// &        - returns memory address of the operand; Ex:  &a returns address of a
// .        - access members of classes or struct variables;  Ex: s1.marks=2
// ->       - used with pointers to access the class or struct variables
// <<       - insertion operator used with cout for output
// >>       - extraction op used with cin for input
