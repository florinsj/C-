// Floating point numbers
// floating point numbers can hold real number such as 420., -3.33, 0.33
// the varying digits can apear before/after decimal point
// the decimal has the ability to float
// there are three float data types : float , double, long double
// in most modern arhitectures: float is 4bytes, double is 8bytes, long double= 8bytes or 16byte
// !! float point data types are always signed ( can hold positive and negative values )

#include <iostream>
using namespace std;
int main()
{
    double temp = 4.22;

    return 0;
}
