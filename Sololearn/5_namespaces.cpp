#include <iostream>

using namespace std;

// a namespace is a declarative region that provide a scope to the identifiers inside it

/* used to organize code into logical groups to prevent name collisions when your code
includes multiple libraries */

// identifiers = names of elements
// using namespace std-> tells the compiler to use standard namespace std(include C++ Standard Library)

int main()
{

    cout << "C++ is cool";
    return 0;
}
