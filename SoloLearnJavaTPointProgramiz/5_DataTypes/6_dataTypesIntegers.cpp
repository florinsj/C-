// Sololearn
// Integers
// Integers =non-fractional numbers, positive or negative
// the size of interger varies according the arhitecture of system on wich the program runs
// the minimum size in most modern arhitecture= 4bytes (32bits)
#include <iostream>
using namespace std;
int main()
{
    int a = 42;

    // several basic types can be modified by modifiers:
    // signed   - signed integer can hold both positive and negative numbers
    // unsigned - unsigned integer can hold just positive numbers
    // short    - short integer can hold half of of default size
    // long     - long integer can hold twice of default size

    unsigned long int b; // only positive integers , twice the default size

    return 0;
}
